<h2>List of ratings</h2>

<p> Number of ratings <%= @ratings.count %> </p>

<ul>
  <% @ratings.each do |rating| %>
  <li> <%= rating.to_s %> <%= link_to 'delete', rating, method: :delete, data: { confirm: 'Are u sure?'} %> </li>
      <% end %>
</ul>

<%= link_to 'New Rating', new_rating_path %>
